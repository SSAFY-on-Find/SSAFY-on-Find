spring:
  application:
    name: chelsea

  profiles:
    active: ${SPRING_PROFILES_ACTIVE}
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB
      max-request-size: 55MB

server:
  port: 8080
  servlet:
    context-path: /api/v1

