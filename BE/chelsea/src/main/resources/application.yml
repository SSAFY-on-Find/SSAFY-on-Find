spring:
  application:
    name: chelsea
  profiles:
    include:
      - local
      - swagger
  config:
    import: optional:file:.env[.properties]
  servlet:
    multipart:
      enabled: true

server:
  port: 8080
  servlet:
    context-path: /api/v1

